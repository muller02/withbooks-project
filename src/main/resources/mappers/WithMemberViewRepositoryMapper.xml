<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.withbooks.web.repository.WithMemberViewRepository">

	<select id="findByWithId" resultType="WithMemberView">
		select *  from with_member_view where with_id =#{withId}
	</select>



</mapper>