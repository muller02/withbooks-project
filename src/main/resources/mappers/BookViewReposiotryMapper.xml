<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.withbooks.web.repository.BookViewRepository">
	
	<select id="findAll" resultType="BookView">
		SELECT 
		*
		FROM book_view
		<where>
			<if test="query != null and query != '' ">
				and title like '%${query}%'
			</if>
			<if test="categoryId != null">
				and category_id = ${categoryId}
			</if>
		</where>
			
				<!-- <where>
			<if test="categoryId != null">
				category_id = #{categoryId}
			</if>
			<if test="query != null">
				and kor_name like '%${query}%'
			</if>
		</where>
		limit #{offset}, #{size} -->
	</select>
	
	<select id="findById" resultType="BookView" parameterType="Long" >
		SELECT 
		*
		FROM book_view
		WHERE ID = #{id}
	</select>

	<!-- <select id="count" resultType="int">
		SELECT 
		count(id)
		FROM menu
		<where>
			<if test="categoryId != null">
				category_id = #{categoryId} 
			</if>
			<if test="query != null">
				and kor_name like '%${query}%'
			</if>
		</where>
	</select> -->


</mapper>